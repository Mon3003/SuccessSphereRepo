App As appinfo:
    BackEnabled: =true
    OnStart: =Set(gblFont, Font.Arial)
    StartScreen: =
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

